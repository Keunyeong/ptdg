let currentScore = 0;
let diceNum = // 랜덤 주사위로 나온 숫자

if (diceNum === 1) {
    currentScore = 0;
    // 다음 사람 턴
}   else {
    currentScore += diceNum;
        if (currentScore >= 100) {
            // 게임 종료
        } else {
            if(// 실행 o) {
                // 주사위 함수 실행
            } else {
                // 다음 사람 턴
            }
        }
}